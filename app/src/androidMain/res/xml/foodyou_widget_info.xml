<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is the framework's 30-minute floor (anything lower is silently clamped). It
  also bounds two things the widget relies on: how stale "today" can get across midnight, and how
  long an Unavailable render persists before the next automatic retry.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minHeight="110dp"
    android:minWidth="250dp"
    android:previewImage="@mipmap/ic_launcher"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="4"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
